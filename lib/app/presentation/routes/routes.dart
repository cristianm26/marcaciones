class Routes {
  /// `Routes._();` es un constructor privado de la clase `Routes`. Se utiliza para evitar que la clase
  /// sea instanciada desde fuera de la clase. Esta es una práctica común en Dart para crear una clase
  /// singleton.

  Routes._();
  static const splash = '/splash';
  static const singIn = '/sign-in';
  static const home = '/home';
  static const offline = '/offline';
}
